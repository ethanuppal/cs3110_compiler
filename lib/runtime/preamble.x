namespace std {
    @decl print_int(Int)
    @decl print_bool(Bool)
    @decl print_char(Char)
    @decl print_string(Char*)
    @decl print_endline()

    namespace math {
        @decl inc(Int) -> Int
        @decl add(Int, Int) -> Int
    }
}
